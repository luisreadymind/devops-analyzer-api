# ================================================================
# CONFIGURACIÓN DE DEPLOY PARA DEVOPS-ANALYZER-API
# Copiar a .env.deploy y configurar valores reales
# ================================================================

# GitHub Token para Container Registry (REQUERIDO)
# Obtener en: https://github.com/settings/tokens
# Permisos necesarios: read:packages, write:packages, delete:packages
GITHUB_TOKEN=your_github_token_here

# Azure Configuration (OPCIONAL - usa defaults si no se especifica)
AZURE_SUBSCRIPTION_ID=your_subscription_id
AZURE_RESOURCE_GROUP=devops-analyzer-api
AZURE_APP_NAME=devops-analyzer-api

# Docker Registry Configuration (OPCIONAL - usa defaults)
DOCKER_REGISTRY=ghcr.io
DOCKER_USERNAME=luisalbertoarenas

# Build Configuration (OPCIONAL)
NODE_ENV=production
BUILD_CACHE=true
SKIP_TESTS=false

# Logging Level (OPCIONAL)
LOG_LEVEL=info

# ================================================================
# INSTRUCCIONES DE USO:
# 
# 1. Copiar este archivo: cp .env.deploy.example .env.deploy
# 2. Editar .env.deploy con valores reales
# 3. Cargar variables: source .env.deploy
# 4. Ejecutar deploy: ./deploy-analyzer-api.sh
# 
# O en una línea:
# source .env.deploy && ./deploy-analyzer-api.sh
# ================================================================